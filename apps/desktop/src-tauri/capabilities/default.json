{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for Roughcut Stdio.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
