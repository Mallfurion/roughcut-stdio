{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for Timeline Cutter.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
